layout: monolith
validation: auto
defaults:
  runtime: rag-turnkey-query
  module: rag-turnkey-query
  synthetic: { module: rag-turnkey-query }
modules:
  rag-turnkey-query:
    runtime: rag-turnkey-query
    steps: [prepare-embedding, embed-question, prepare-vector-search, search-vectors, build-retrieved-context, draft-answer, validate-citations]
